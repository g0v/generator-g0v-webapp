language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-g0v-webapp' ]; then cd .. && eval "mv $currentfolder generator-g0v-webapp" && cd generator-g0v-webapp; fi

